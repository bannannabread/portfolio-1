<section class="hero">
  <div class="wrap">
    <h1 class="reveal" style="--delay: 0s">Megan &mdash; a product designer who engineers.</h1>
    <p class="lede reveal" style="--delay: 0.08s">
      I design and ship product experiences end to end &mdash; from the research
      and interaction decisions to the code that carries them into production.
    </p>
    <div class="cta-row reveal" style="--delay: 0.16s">
      <a class="cta" href="/work">See the work</a>
      <a class="cta cta--ghost" href="/contact">Get in touch</a>
    </div>
  </div>
</section>

<style>
  .hero {
    padding-block: var(--space-6) var(--space-6);
  }

  h1 {
    max-width: 18ch;
  }

  .lede {
    margin-top: var(--space-3);
    font-size: 1.1875rem;
    color: var(--ink-soft);
    max-width: 52ch;
  }

  .cta-row {
    display: flex;
    gap: var(--space-2);
    margin-top: var(--space-4);
    flex-wrap: wrap;
  }

  /* One authored entrance moment for the hero only — not repeated per section. */
  .reveal {
    animation: rise-in 0.7s var(--ease-out) both;
    animation-delay: var(--delay);
  }

  @keyframes rise-in {
    from {
      opacity: 0;
      transform: translateY(0.75rem);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .reveal {
      animation: none;
    }
  }

  .cta {
    display: inline-flex;
    align-items: center;
    padding: 0.85rem 1.5rem;
    border-radius: 999px;
    background: var(--accent);
    color: var(--ground);
    text-decoration: none;
    font-size: 0.9375rem;
    transition: background 0.2s var(--ease-out), transform 0.2s var(--ease-out);
  }

  .cta:hover {
    background: var(--ink);
    color: var(--ground);
    transform: translateY(-0.1rem);
  }

  .cta--ghost {
    background: transparent;
    color: var(--ink);
    border: 1px solid var(--ground-deep);
  }

  .cta--ghost:hover {
    border-color: var(--ink);
    background: transparent;
  }

  @media (max-width: 640px) {
    .hero {
      padding-block: var(--space-5) var(--space-5);
    }
  }
</style>
