<section class="hero">
  <div class="wrap">
    <h1 class="reveal" style="--delay: 0s">Megan &mdash; a product designer who <span class="accent-word">engineers</span>.</h1>
    <p class="lede reveal" style="--delay: 0.08s">
      I build products end to end, connecting deep UX research with modern AI
      workflows to turn complex ideas into production-ready software.
    </p>
    <div class="cta-row reveal" style="--delay: 0.16s">
      <a class="cta" href="#work">See the work</a>
      <a class="cta cta--ghost" href="/contact">Get in touch</a>
    </div>
  </div>
</section>

<style>
  .hero {
    position: relative;
    z-index: 0;
    overflow: hidden;
    padding-block: var(--space-6) var(--space-6);
  }

  .hero::before,
  .hero::after {
    content: '';
    position: absolute;
    z-index: -1;
    border-radius: 50%;
    filter: blur(4.5rem);
    pointer-events: none;
  }

  .hero::before {
    width: 28rem;
    height: 28rem;
    top: -10rem;
    right: -6rem;
    background: radial-gradient(closest-side, var(--card-peach), var(--card-coral) 70%, transparent 100%);
    opacity: 0.55;
  }

  .hero::after {
    width: 20rem;
    height: 20rem;
    bottom: -8rem;
    left: -4rem;
    background: radial-gradient(closest-side, var(--card-violet), var(--card-rose) 70%, transparent 100%);
    opacity: 0.4;
  }

  h1 {
    max-width: 18ch;
  }

  .accent-word {
    color: var(--accent);
  }

  .lede {
    margin-top: var(--space-3);
    font-size: 1.1875rem;
    color: var(--ink-soft);
    max-width: 52ch;
  }

  .cta-row {
    display: flex;
    gap: var(--space-2);
    margin-top: var(--space-4);
    flex-wrap: wrap;
  }

  /* One authored entrance moment for the hero only — not repeated per section. */
  .reveal {
    animation: rise-in 0.7s var(--ease-out) both;
    animation-delay: var(--delay);
  }

  @keyframes rise-in {
    from {
      opacity: 0;
      transform: translateY(0.75rem);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .reveal {
      animation: none;
    }
  }

  @media (max-width: 640px) {
    .hero {
      padding-block: var(--space-5) var(--space-5);
    }
  }
</style>
